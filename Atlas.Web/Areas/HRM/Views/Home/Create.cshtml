@model Employee

@{
    ViewData["Title"] = "Create Employee";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8 col-xl-6">
            <div class="card shadow-sm border-0">
                <div class="card-body p-4 p-md-5">
                    <div class="mb-4">
                        <h1 class="h3 mb-1">Create Employee</h1>
                        <p class="text-muted mb-0">Tạo nhân viên mới bằng quyền HR_MANAGE.</p>
                    </div>

                    <form asp-area="HRM" asp-controller="HRM" asp-action="Create" method="post">
                        @Html.AntiForgeryToken()

                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="EmployeeNumber" class="form-label"></label>
                            <input asp-for="EmployeeNumber" class="form-control" />
                            <span asp-validation-for="EmployeeNumber" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="PersonId" class="form-label"></label>
                            <input asp-for="PersonId" class="form-control" type="number" min="1" />
                            <span asp-validation-for="PersonId" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Create</button>
                            <a asp-area="HRM" asp-controller="HRM" asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>